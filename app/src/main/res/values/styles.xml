<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="RoundedImageView" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">12dp</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/latoregular</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:editTextColor">@color/text_primary</item>
    </style>
    <style name="BottomNavigationActiveIndicatorStyle">
        <item name="android:width">56dp</item> <!-- Adjust size based on your icon size -->
        <item name="android:height">56dp</item>
        <item name="android:shape">oval</item>
        <item name="android:color">@color/primary_light</item> <!-- Use your desired color -->
    </style>

    <!-- Add your other existing styles here -->
</resources> 